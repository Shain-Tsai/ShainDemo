{
    "routes": [
        {
            "route": "/admin/*",
            "allowedRoles": ["administrator"]
        },
        {
            "route": "/login",
            "rewrite": "/.auth/login/aad"
        }
    ]
}